# Base URL of the Django API, including the /api prefix.
# Leave unset in local dev — Vite proxies /api to http://localhost:8000.
# On Vercel, set this to your deployed backend, e.g.:
# VITE_API_URL=https://shredtrainer-api.up.railway.app/api
VITE_API_URL=
